"""Reserved module for future sandboxed execution capabilities.

Intended to house capability-check helpers and sandbox configuration
(e.g., exec_host sandboxing profiles, resource-limit enforcement) once
those features are promoted from config flags to first-class runtime objects.
Currently unused — do not import from this package.
"""
